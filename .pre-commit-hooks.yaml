- id: context-map
  name: ContextMap Auto Sync
  description: Escanea y actualiza automáticamente la bóveda de Obsidian y briefs de ContextMap antes de cada commit.
  entry: python -m context_map.cli build --clean --brief --quiet
  language: python
  stages: [pre-commit]
  types: [python, markdown, yaml, json]
